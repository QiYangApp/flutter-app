import 'package:flutter/cupertino.dart';

class LoginFromView extends StatelessWidget {
  const LoginFromView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Login");
  }

}